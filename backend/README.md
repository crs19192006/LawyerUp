# Skill Intelligence API

## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Notes
- AI logic is mocked and labeled with TODOs.
- Replace mocked services with Gemini integrations as needed.
