"use client";

import LoginForm from "../../../components/LoginForm";

export default function ClientLoginPage() {
  return (
    <LoginForm
      role="client"
      title="Client Login"
      subtitle="Quick demo access for verified and new clients."
    />
  );
}
